/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function (knex) {
  return knex.schema.table('calculated_orders', (table) => {
    table.json('mealDetails');
  });
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function (knex) {
  return knex.schema.table('calculated_orders', (table) => {
    table.dropColumn('mealDetails');
  });
};
